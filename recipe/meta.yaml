{% set name = "AWSIoTPythonSDK" %}
{% set version = "1.6.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | lower }}-{{ version }}.tar.gz
  sha256: 498f7f96466c9a8030f1b97cb683579d21e8c2f84435f86b4db379ef4531aabf

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6

test:
  imports:
    - AWSIoTPythonSDK
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/aws/aws-iot-device-sdk-python.git
  summary: SDK for connecting to AWS IoT using Python.
  license: Apache-2.0
  license_file:
    - LICENSE.txt
    - NOTICE.txt

extra:
  recipe-maintainers:
    - Rama2209
